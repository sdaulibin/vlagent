{{- if .Values.config }}
{{- $config := .Values.config | nindent 4 }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: vlagent-backend-config
  namespace: {{ .Release.Namespace }}
data:
  config.yml: |
    {{- $config }}
{{- end }}
